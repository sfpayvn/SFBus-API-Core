import { Model, Types } from 'mongoose';
import { SearchTenantQuerySortFilter, SearchTenantsRes, TenantDto } from './dto/tenant.dto';
import { CreateTenantDto } from './dto/create-tenant.dto';
import { UpdateTenantDto } from './dto/update-tenant.dto';
import { TenantDocument } from './schema/tenant.schema';
import { TenantSubscriptionService } from '../tenant-subscription/tenant-subscription.service';
import { FileService } from '../file/file/file.service';
import { UserService } from '../user/user/user.service';
import { SubscriptionService } from '../subscription/subscription.service';
import { SettingsService } from '../settings/settings.service';
export declare class TenantService {
    private readonly tenantModel;
    private readonly tenantSubscriptionService;
    private readonly fileService;
    private readonly userService;
    private readonly subscriptionService;
    private readonly settingsService;
    constructor(tenantModel: Model<TenantDocument>, tenantSubscriptionService: TenantSubscriptionService, fileService: FileService, userService: UserService, subscriptionService: SubscriptionService, settingsService: SettingsService);
    create(createTenantDto: CreateTenantDto): Promise<TenantDto>;
    update(updateTenantDto: UpdateTenantDto): Promise<TenantDto>;
    delete(id: Types.ObjectId): Promise<boolean>;
    findAll(): Promise<TenantDto[]>;
    validateTenant(phoneNumber: string): Promise<TenantDto | null>;
    findOne(id: Types.ObjectId): Promise<TenantDto | null>;
    findByCode(code: string): Promise<TenantDto>;
    findByPhoneNumber(phoneNumber: string): Promise<TenantDto>;
    search(pageIdx: number, pageSize: number, keyword: string, sortBy: SearchTenantQuerySortFilter, filters: SearchTenantQuerySortFilter[]): Promise<SearchTenantsRes>;
    buildQuerySearchTenants(pageIdx: number, pageSize: number, keyword: string, sortBy: SearchTenantQuerySortFilter, filters: SearchTenantQuerySortFilter[]): Promise<any>;
    private mapLogoUrl;
}
